# JLC CLI 需求规范

## 1. 总体目标

cli 实现正常进入jlc.com的所有操作,

## 2. CLI 启动与交互

### 2.1 启动命令

terminal jlc-cli进入，仅jlc-cli不带参数，tui页面有类似Codex/claude/agy那样，会显示一个jlc的图表和下面的输入框

## 3. AI CLI 规范

需要符合给AI使用的CLI规范

## 4. 核心功能

### 4.1 PCB 下单与查单

先做PCB下单(使用本地gerber/pcb文件)（下单的时候支持导出预览图效果），查单（看订单扭转到那个状态了）

可以根据自己要求（可以支持多张要求文件来进行下单），执行选择下单参数

## 5. 首次使用警告

需要有jlc-com第一次进行，需要有warning，类似claude在setting中设置了full access后第一次进入那样，警告信息设置为中文的，当前cli为非官方插件，使用当前cli请自行承担其后果，

## 6. Mode 模式

支持两种mode，这个mode主要是给AI Agent使用的时候注入的prompt/skill.md吧，并且cli下有类似claude的mode显示即可，cli也支持切换mode

- 严谨模式（manual），对于所有的生产工艺都需要自己用户给相关依据（如给了下单工艺pdf/xlsx/明确prompt中有提到）
- ai选择模式（auto），对于用户给出的工艺就按照用户的工艺来，对于用户每给出来的，就AI自主选择（依据用户过去的下单习惯/全局偏好）

改为simple/hard mode，名字上

## 7. Hook 与通知

功能需要

支持hook？对于下单了的订单会去查询订单状态，支持添加各种通知
