# 嘉立创 CLI 需求文档（PRD）

## 1. 产品目标与范围

快速上线第一版：使用 [Obscura](https://github.com/h4ckf0r0day/obscura) 封装本 PRD 列明的嘉立创业务操作，提供人类和 AI Agent 都可调用的 CLI。

- 最终交付是让 AI Agent 快速使用嘉立创服务的业务 CLI：Agent 直接调用上传、参数设置、报价、建单、余额支付及查询等功能，常规流程由 CLI 完成，无需 Agent 逐步通过浏览器/CDP 点击页面。
- 完整交付物为“CLI + 配套 Skill”。Skill 随 CLI 一同发布，让首次接触项目的 AI Agent 能快速理解能力、选用命令并处理结果；它是 v1 必需交付项。
- 发布平台覆盖 macOS、Windows、Linux，提供各平台可安装、可运行的 CLI 及使用说明。
- “所有功能”指本 PRD 明确列出的功能，不自动扩大为嘉立创网站的全部服务。
- 目标站点确定为 `jlc.com`（嘉立创中国站）。
- 自动化执行以 Obscura 为基础；流程超时、出现预期外情况或无法继续时，支持 AI Agent 通过 CDP 连接对应浏览器会话处理。
- 参数决策以人为基础。默认模式不得替用户决定未明确确认的参数。
- 本 PRD 的 v1 功能完成并通过验收后，发布第一版。
- 初始化授权的呈现方式可参考旧项目；其余设计和实现不参考旧项目。
- 用户已授权由实现者自行解决开发问题并作出工程决策。原待确认项的取舍记录于第 11 节；这些决策不构成实际账户的下单、扣款或对外发布授权。

## 2. v1 功能清单

| 编号 | 功能 | 交付结果 |
| --- | --- | --- |
| F01 | 初始化与人类授权 | 展示授权说明，记录人类明确确认的结果 |
| F02 | 多方式登录与登录状态复用 | 读取可用登录方式，交付当前扫码二维码，核实登录身份后返回成功结果，并跨 CLI 调用复用有效状态 |
| F03 | 用户信息查询 | 返回账号可访问的客编、电话、手机号等约定字段 |
| F04 | 页面原生支持的 PCB/Gerber 文件上传 | 返回本次上传及解析状态，失败时给出可处理原因 |
| F05 | 下单参数读取与设置 | 展示参数、候选值及约束，按模式规则设置并回读核对 |
| F06 | 报价 | 返回对应文件和参数的报价信息，不将报价等同于付款 |
| F07 | Gerber 解析预览 | 提供与本次上传文件对应的可查看预览结果 |
| F08 | 检查并提交订单 | 核对提交内容，提交后取得并验证对应订单号 |
| F09 | PCB/FPC 订单查询 | 查看订单列表、详情、状态与进度 |
| F10 | 未付款订单支付 | 仅支持嘉立创余额支付；付款必须经人类确认，并核实最终支付状态 |
| F11 | default/auto 参数模式 | 按用户决策与授权规则处理参数 |
| F12 | 嘉小智对话 | 通过 CLI 发送消息并取得对应回复 |
| F13 | 异常报告、CDP 接管与恢复 | 保留任务上下文，支持接管，并在处理后继续或明确结束任务 |
| F14 | 配套 Agent Skill | 提供可加载的 Skill、按需查阅的工具说明及实际可执行示例，覆盖 F01–F13 的使用 |

“下单页所有参数都可以调整”保留为 F05 的覆盖要求：以目标站点选定下单流程中实际可用的参数项为准，包含条件触发后出现的参数；不是要求任意参数组合都能成功提交。

PCB/FPC 订单相关范围目前限定为上表列明的查询及支付功能。取消、退款、发票等未列出的操作不自动纳入；需要时由用户补充进 PRD。

## 3. 初始化、登录与账号信息

### 3.1 初始化授权

- 首次初始化展示授权说明，由人类明确确认。
- 授权说明应准确表达实际权限，不能把“允许工具运行”“保存登录状态”“提交订单”“余额扣款”混为同一项未说明的权限。
- 初始化授权不能代替具体付款的人类确认；每次付款必须先取得人类对该次付款的明确确认。
- 提交未付款订单须先检查并明确调用提交命令；不增加与付款相同的独立确认凭据，详见第 11 节 Q2。

### 3.2 登录和状态复用

- 支持完成登录，并通过账号状态或账号信息确认登录成功，不能只凭页面跳转判断。
- 支持选择目标页面实际开放的多种登录方式；先读取可用方式，再按调用方选择进入流程。不得将隐藏、不可用或尚未验证的方式宣称为已支持。
- 微信快捷登录与二维码登录分别处理：快捷方式使用网站提供的明确按钮，扫码方式在必要时切换其他账号界面并交付真实二维码。若网站要求注册或绑定身份，须明确返回待用户决定，不能将原登录请求当作创建或绑定账号的同意。
- 扫码登录须向调用方交付可展示的当前二维码素材或对应页面信息，关联本次登录任务、页面与生成时间，供人类扫码；不能只输出“请扫码”而没有可用二维码。
- 二维码过期、刷新或登录方式切换后使旧二维码失效；返回新的扫码状态及素材，不把旧二维码当作仍可使用。
- 登录过程区分等待扫码、等待人类输入或验证、二维码过期、登录成功和失败。短信、密码等敏感输入不写入普通日志或任务快照。
- 成功回调给调用 Agent 的结果必须来自当前账号身份或登录有效性核实，包含任务标识和登录状态。支持等待后返回最终 JSON、即时输出 JSONL 进度事件，以及调用方明确配置的 HTTP 回调；进度事件可交付当前二维码，外部回调不携带会话凭据或个人资料。
- 可选 HTTP 回调仅允许本机回环地址或 HTTPS，使用事件 ID 去重并支持由环境变量提供密钥的 HMAC 签名。投递状态持久化，失败可独立重试，不因重试通知重新执行登录或业务操作。
- 保存必要的登录状态，后续调用在状态有效时直接复用。
- 登录状态失效时明确返回需要重新登录，不得继续按已登录状态执行。
- 发生滑块、短信等验证挑战时，识别并报告当前验证步骤，保留现场，支持处理后继续。
- 不把“验证容易绕过”作为产品可用性或验收前提；人类在保留的同一页面完成短信、滑块或扫码等验证后，CLI 再核实实际状态。
- 会话凭据不得出现在普通输出、诊断日志或提交到代码仓库的文件中。

### 3.3 用户信息

- 获取当前账号可访问的客编、电话、手机号等信息。
- 网站只提供脱敏值时如实返回，不承诺获取完整原值。
- 不可用字段明确标注，不编造、不用其他字段替代。
- v1 读取页面可见的客编、电话、手机号及当前账号标识；支持隔离账号配置和退出当前 CLI 配置的本地会话，见第 11 节 Q7。连接外部 CDP 时，不替用户退出外部浏览器账号，结果须明确报告外部会话仍保留。

## 4. 参数选择与决策规则

### 4.1 default 模式（默认）

- 用户未明确确定的参数，AI 不得自行选择、补写或当作已经确认。
- CLI 可以读取网站默认值、展示可选项、说明冲突并提供建议；展示或建议不等于用户已确认。
- 网站预选了某个值，不代表用户已作出该选择。影响本次下单的待决策项应向用户呈现。
- 缺少明确选择时，返回缺失项、当前候选值以及需要用户决定的内容，等待明确输入后继续。
- 不能为了让流程跑通而静默使用网站默认值、上次订单参数或 AI 推测值。
- 用户已明确指定的值不得被静默覆盖。网站拒绝该值、自动修正或因其他选项改变而重置时，应报告差异并要求重新明确选择。
- 由上传文件解析得到的事实可以读取和展示；解析事实与需要用户选择的生产参数应分开表达。

### 4.2 auto 模式

- 用户先给出意图，明确让 AI 帮助选择或填写参数，AI 才按照该意图进行填写。仅仅缺参数，不构成自行补全的依据。
- 已知用户约束和明确选择具有优先级，AI 不得违背或静默修改。
- AI 提议的参数及其依据应可查看。
- 上述决策约束主要通过工具文字描述、帮助文档和 Agent 使用规则表达；不要求 CLI 理解自然语言并判断用户是否已经授权 AI 选填。
- 工具描述应明确说明：default 下不得替用户补填未明确决定的参数；只有用户表达让 AI 帮助选填的意图后，才按 auto 规则补全。
- CLI 负责执行传入参数、检查取值及依赖关系、回读实际生效结果，并返回错误或差异。模式字段本身不能证明 AI 已遵守用户意图。
- 用户让 AI 帮助选填后，不额外规定每个 AI 选择都必须逐项确认；遇到无法判断、约束冲突或需要超出用户意图的情况，应向用户询问。
- v1 不内置模型；由外部 AI Agent 按用户意图作出参数建议并调用 CLI，见第 11 节 Q1。
- auto/default 是参数选择模式；它们本身不构成提交订单或付款的授权。

示例：“按低成本打样帮我选择剩余工艺参数”表达了让 AI 帮助选填的意图；“上传这个文件查看一下”没有表达该意图，AI 不应因此替用户选择生产参数。

### 4.3 参数能力与结果核对

- 支持读取当前流程可用的参数、候选值、单位、必填要求与依赖关系。
- 设置参数后核对页面实际生效值，报告设置失败、值变化和组合冲突。
- 输出最终参数及来源，区分用户明确选择、文件解析、网站预选及 AI 建议/决策。
- 参数清单应通过目标页面核对形成，并纳入验收，不在本文凭空枚举网站能力。

## 5. 文件、报价、预览与订单提交

### 5.1 文件上传和解析

- 仅支持 jlc.com 对应下单页面原生接受的文件，直接上传并使用网站解析能力；CLI 不额外承担 EDA 源文件转换。
- 具体格式、压缩包结构和大小限制以目标页面实际要求为准；开发时核对并写入使用说明，不凭扩展名推断网站必然支持。
- 对不支持的输入返回明确原因和页面支持范围，不静默转换文件或替换上传内容。
- 检查文件是否存在、是否可读取，关联本次上传任务与源文件。
- 追踪上传和网站解析结果；解析失败、超时或出现预期外页面时进入异常处理流程。
- 不把文件上传成功等同于解析成功或可下单。

### 5.2 预览

- 提供 Gerber 解析预览，并明确对应哪个文件和上传任务。
- 预览生成失败或尚未完成时返回实际状态，不使用旧文件预览替代。
- 预览采用网站解析结果，交付网站可见的预览页面链接或可查看素材；只返回实际存在的视角，不额外进行本地 Gerber 渲染，见第 11 节 Q4。

### 5.3 报价

- 基于当前文件与已确定参数获取报价，记录报价对应的参数和时间。
- 返回网站提供的金额、币种及费用明细；无法确定的费用明确说明。
- 区分“已获得报价”“已创建订单”“已付款”三种结果。
- 文件或影响价格的参数变化后重新报价，不把旧报价作为新配置的最终金额。
- 报价阶段是否允许网站产生待付款订单，需要在目标流程核对后明确，不提前假设网站行为。

### 5.4 检查与提交

- 提交前汇总实际文件、参数、金额及流程要求的收货等信息，检查是否存在未解决的缺失项或冲突。
- 按确定后的授权规则执行提交。
- 提交后取得对应订单号，并查询核对订单存在且关联本次任务。
- 报价或提交内容出现变化时报告差异，不能继续沿用与实际内容不符的确认结果。
- 结果不明时进入核实流程，不能直接重复提交。

## 6. 订单查询与支付

### 6.1 PCB/FPC 订单查询

- 支持订单列表和指定订单详情查询，展示订单状态及进度。
- 如实返回网站可见信息，区分订单状态、支付状态与生产进度。
- v1 支持当前页面可见的 PCB/FPC 订单查询；筛选仅覆盖实际可用且已核对的页面条件。FPC 新下单不纳入本版，见第 11 节 Q7。

### 6.2 余额支付

- v1 仅支持对指定未付款订单使用嘉立创余额支付。
- 每次付款前必须有人类明确确认。先呈现账号、订单、应付金额及嘉立创余额支付方式，取得针对该次付款的确认后才能执行扣款。
- 未取得确认时，返回“需要人类确认”及付款摘要，不能继续付款。初始化授权、auto 模式和 Agent 自行决定均不能替代人类确认。
- 付款确认应绑定本次账号、订单、金额及支付方式；这些内容改变后，原确认不再适用，需要人类重新确认。
- CDP 接管和自动恢复同样遵循付款确认要求，不得因进入恢复流程而跳过确认。若已确认付款但结果不明，先核实是否已经成功，不重复扣款。
- 对余额不足、订单状态变化及网站拒绝等结果明确报告。
- 支付后捕捉实际页面变化，读取对应订单的支付成功提示或已支付状态，核实结果；不能只以点击按钮成功作为付款成功。
- 页面发生变化本身不等于支付成功；弹窗关闭、跳转或加载完成均不能单独作为成功依据。成功信号应关联当前支付任务及订单，避免误读其他订单或旧页面的结果。
- CLI 识别页面上的支付成功状态后，将明确的结构化成功结果返回给调用它的 AI Agent，让 Agent 知道余额付款已完成。
- 返回结果包含任务 ID、订单号、支付状态，以及页面可核实的金额、支付方式和成功依据；不可读取的字段不编造。人类可读输出也应明确显示支付成功。
- 处理等待超时及进程退出，不能将等待超时直接报告为支付失败。
- 页面没有明确成功信号或状态存在冲突时，继续核实或返回“结果待确认”；流程超时、出现预期外情况时进入 CDP 接管流程，不重复发起付款。

## 7. 流程超时、预期外情况与 CDP 接管

### 7.1 触发条件

- 某一步骤超过约定时间仍未达到预期结果。
- 出现预期外页面、弹窗、验证、错误或参数变化。
- 关键元素不可用，登录失效，或当前状态无法确定。
- 订单提交、支付等操作已经发起，但结果无法核实。

### 7.2 异常报告和现场保留

- 停止当前自动化操作，保留 Obscura 浏览器会话及相关页面，供 CDP 连接处理。
- 返回任务 ID、当前步骤、预期结果、实际现象、错误类别和时间。
- 提供 CDP 连接信息及对应页面标识，并提供可用的截图或页面快照用于定位问题；若诊断材料不可用，应说明原因。
- 明确最近发起的操作是否可能已产生订单或扣款，不把未知结果归为可安全重试。
- 浏览器本身退出或无法继续提供 CDP 时，应明确报告接管不可用及已保留的信息。
- CDP 访问不得因接管需求而无保护地暴露到公网；连接信息与诊断材料需避免泄露会话凭据。

### 7.3 接管与恢复

- AI Agent 可通过 CDP 连接发生问题的同一会话，检查并处理阻塞步骤。
- 接管期间 CLI 不并行操纵相同任务页面，避免双方操作相互干扰。
- 接管权限沿用用户已给出的业务授权，连接 CDP 不自动增加下单或付款权限。
- 接管完成后检查实际页面、账号及业务状态，再决定从哪一步继续，不能盲目重复上一步操作。
- 如接管改变了文件或参数，应重新核对，并使不再匹配的报价或确认结果失效。
- 无法恢复时明确结束原因，并保留已产生的订单号等结果。
- CLI 检测问题已解决后自动恢复原任务，不要求用户或 Agent 额外执行恢复命令。
- “问题已解决”应以当前步骤的预期条件及业务状态为依据，不能仅凭 CDP 断开、页面变化或弹窗消失判断。
- 自动恢复前应完成状态核对，并协调页面控制权，确保接管操作已结束或释放控制后再继续写操作；检测期间不得与 Agent 同时操作页面。
- 检测到问题解决但提交或支付结果仍不明确时，先核实结果，不自动重做相关操作。
- 超时阈值、现场保留及程序退出后的恢复范围采用第 11 节 Q8 的有界等待与显式观察设计；具体默认值与当前实现同步记录在 CLI 帮助及使用文档中。

### 7.4 订单与支付结果不明

- 提交或支付超时后，先核实实际订单/支付状态，再决定是否允许继续。
- 已确认成功的操作不得重复执行。
- 仍无法确认时返回“结果待确认”，保留任务和订单关联信息，交由 Agent 或用户处理。
- 支持区分“安全重试”“需先核实”“需要补充输入”“需要 CDP 接管”，避免调用方无差别重跑。

## 8. 嘉小智对话

- 通过 CLI 向嘉小智发送消息并取得对应回复。
- 区分发送成功、回复生成中、回复完成和失败状态。
- 会话失效、回复超时或页面异常时，遵循统一异常处理规则。
- 嘉小智是业务对话功能；其回复不自动转化为用户确认的下单参数或支付授权。
- v1 每次调用发送一条文本并返回对应回复，可复用当前网站页面会话；不承诺可移植历史、独立会话管理、流式输出或附件，见第 11 节 Q6。

## 9. CLI 与 Agent 交互

### 9.1 CLI 接口与平台支持

- 提供适合人阅读的输出，以及稳定的结构化结果，便于 Agent 判断下一步。
- 结构化结果应包含操作、任务标识、状态、数据、错误及可执行的下一步提示；字段名和命令语法在接口设计阶段确定。
- 正常结果与诊断日志分离；错误应具有可区分的错误类别和退出行为。
- 缺用户参数、缺授权、等待接管等状态应明确报告，不能伪装成成功或在非交互调用中无限等待输入。
- 查询类操作、修改参数、提交订单和支付应能区分，方便调用方明确选择要执行的业务动作。
- 提供 Agent 使用说明，说明业务命令、参数决策规则、付款确认要求、结构化结果和异常接管流程。
- macOS、Windows、Linux 均须支持上述 CLI 功能；各平台的路径、会话存储和 Obscura 启动方式应适配对应系统。
- 各平台具体安装包形式、支持的系统版本与 CPU 架构，以及等待方式和默认超时，在技术验证及发布设计中明确并记录；不将能够编译等同于可用。

### 9.2 配套 Skill

#### 目标与职责

- AI Agent 读取 Skill 后，应能从用户的业务需求判断使用哪些 CLI 命令、还缺哪些输入、是否需要人类确认，以及执行后如何继续。
- CLI 封装 Obscura 的常规页面操作；Skill 负责解释能力、调用方式、决策规则和异常处理，不要求 Agent 为正常流程重新编写逐步点击脚本。
- Skill 覆盖本 PRD 列明的全部 CLI 功能；仅描述当前发布版本真实可用的命令，不将计划功能或旧项目接口写成已支持能力。

#### 组织方式

- 交付独立 Skill 目录，包含具有 `name`、`description` 的 `SKILL.md`；描述应能让 Agent 在需要通过 jlc.com 上传、报价、订单管理、余额支付或嘉小智对话时识别该 Skill。
- `SKILL.md` 保持简洁，包含快速开始、业务到命令的索引、关键决策规则和详细说明入口。
- 较长的参数说明、结果结构、业务示例及异常处理资料放入按需读取的引用文件，由入口明确说明何时查阅，避免每次调用加载全部文档。
- Skill 可与 CLI 一起获取和安装，说明发现 CLI、检查版本和加载 Skill 的方法；示例适用于发布支持的平台，不依赖开发者机器的绝对路径或私有配置。

#### 必须说明的内容

| 内容 | Agent 应获得的信息 |
| --- | --- |
| 快速开始 | 检查 CLI/Obscura 可用性、初始化、人类授权、登录及状态复用，并完成一次简单查询 |
| 业务命令索引 | 用户要做什么、使用什么命令、必要前置条件及下一步；覆盖 F01–F13 |
| 输入与参数 | 页面原生文件范围、必填项、候选值与依赖关系的查询方式、路径及参数传递方法 |
| default/auto | default 不擅自补填；auto 先有用户让 AI 帮助选填的意图，遇到冲突或意图不明确时询问 |
| 结果读取 | JSON 字段、任务与订单标识、状态、退出码、错误和下一步提示的含义 |
| 常规业务示例 | 上传与预览、设置参数与报价、提交订单、查询状态、余额支付、嘉小智对话；区分可独立执行和需授权的步骤 |
| 付款确认 | 如何向人类呈现具体订单及金额、如何按实际接口传递确认；不得代替人类确认，auto 和初始化授权不能免除确认 |
| 支付结果 | 读取对应订单支付成功结果；待确认、失败及超时如何处理，避免重复扣款 |
| CDP 接管 | 根据 CLI 返回信息连接对应会话、定位异常、处理阻塞并配合控制权交接；CLI 检测问题解决后自动恢复 |
| 异常恢复 | 缺输入、登录失效、页面变化、解析失败、流程超时及结果不明的处理；哪些可重试、哪些需先核实 |
| 版本与限制 | Skill 对应的 CLI 版本、平台差异、已知限制和版本不匹配时的处理 |

#### 维护要求

- 命令、参数、结果结构或恢复行为变更时，同步更新 Skill 及关联示例，随对应 CLI 版本发布。
- 决策说明主要由 Skill 承载，CLI 的帮助和错误提示提供相应入口；付款确认等执行约束不能只靠 Skill 文字宣称已实现。
- Skill 不包含账号凭据、真实支付确认信息或个人数据；示例数据与实际业务结果明确区分。

## 10. v1 验收要求

- F01–F14 均有对应的可执行验收步骤与实际结果记录；不以命令存在、页面可打开或模拟结果代替真实流程验证。
- 登录后再次启动 CLI 能复用有效状态；过期状态能正确识别。
- 逐项核对页面开放的登录方式；扫码方式实际交付可扫描的当前二维码，核验二维码过期或切换后的失效处理；只有读取当前登录身份后才向 Agent 返回登录成功。记录人类完成验证与 CLI 核实的边界。
- 工具描述与 Agent 调用流程需验证：default 下不替用户补填未明确决定的参数；auto 下先有用户让 AI 帮助选填的意图，再执行填写。不能仅凭 CLI 收到一个模式字段就宣称决策约束已通过验收。
- CLI 层验证参数执行、依赖检查及实际值回读；Agent 层验证是否遵守用户意图。CLI 不承担自然语言授权识别。
- 页面原生支持的文件能完成上传和解析；不支持的输入有明确错误，不执行额外源文件转换。上传、解析、预览及报价能关联同一文件和参数；参数变化后的结果正确更新。
- 提交成功有可查询的订单号；提交授权按 Q2 确定后的规则执行。实际付款验收必须先取得人类对该次付款的明确确认。
- 未获人类确认时不会扣款；确认后账号、订单、金额或支付方式发生变化时不会沿用旧确认。auto 模式、CDP 接管及自动恢复均不能跳过该要求。
- 余额支付完成后，CLI 能捕捉对应页面的成功状态并向 AI Agent 返回可识别的成功结果；余额不足、无关页面跳转和等待超时不会被误报为支付成功。
- 在超时或预期外情况中，Agent 能实际连接对应 CDP 会话并处理问题；CLI 检测问题解决、核对状态和页面控制权后自动恢复，无需额外恢复命令。
- 接管期间不会发生 CLI 与 Agent 争抢页面操作。
- 提交或支付返回结果丢失时，不会盲目重复操作。
- 嘉小智对话和订单查询按确定的范围验证成功与失败路径。
- macOS、Windows、Linux 均有安装、启动、Obscura 连接及业务功能的运行验证记录，逐平台标明验证环境和结果；未验证的平台不得标为已验收。
- Skill 的元数据及内部引用有效，说明中的命令、参数和结果示例与交付 CLI 一致；不能只有 PRD 或一份没有实际调用方式的介绍文档。
- 首次接触项目的 Agent 仅凭配套 Skill、CLI 帮助和正常所需用户输入，即可完成初始化/登录引导、文件报价及订单查询，无需阅读源代码或依赖开发者口头补充；记录调用步骤和实际结果。
- 验证 Skill 能引导 Agent 在 default 缺参数时询问、auto 有用户意图时选填、付款前请求人类确认、结果不明时先核实，并按需使用 CDP 接管；常规流程不要求逐步操纵浏览器页面。
- 落实第 11 节工程决策，冻结验收范围并记录已知限制后，才能判断 v1 是否满足上线条件。

## 11. 开发授权后的工程决策

以下是依据用户“开发过程中如有任何问题都你自己去解决”的授权作出的 v1 取舍，用于冻结实现范围。授权仍不替代实际业务流程中的用户意图、具体付款确认或网站真实结果核验。

| 编号 | v1 决策 |
| --- | --- |
| Q1 | CLI 不内置模型；由外部 Agent 使用配套 Skill 理解用户意图并决定参数。CLI 负责取值验证、实际执行与回读。 |
| Q2 | 建立未付款订单必须先生成并核对提交摘要，再明确调用提交命令，不添加额外人类确认凭据。提交行为仍须属于用户授权的业务范围；报价不会自动提交，提交不会自动付款。每笔余额支付另经人类确认。 |
| Q3 | 接管使用有期限的页面控制租约；观察流程先只读检测实际状态。租约释放或到期且前置条件重新核实后，尚未发起的参数设置、提交或付款步骤可以在原授权与绑定约束内自动继续，无需 resume 命令。已有提交或扣款意图记录时仅核实结果，不重做。业务观察器须运行中，独立浏览器中继仅负责保留会话。 |
| Q4 | 只使用 jlc.com 原生文件解析与预览；返回当前上传关联的网站预览链接或素材及可读取的视角，不增加转换器或本地渲染器。 |
| Q6 | 一次命令发送一条文本并等待相应回复，可继续使用同一网站页面会话；不交付流式协议、附件、可移植对话历史或独立多会话管理。 |
| Q7 | 查询当前页面可见的账号标识、客编、电话、手机号，保留网站脱敏状态；增加隔离配置与本地退出。退出清除当前 CLI 配置的本地会话，停止 CLI 自有浏览器；外部 CDP 会话不注销，须明确报告。订单查询覆盖页面可见 PCB/FPC 数据及已核对的筛选项；不做 FPC 新下单。 |
| Q8 | 通过 npm 包交付 CLI 与 Skill，Node.js 最低版本 22.12，目标平台 macOS、Windows、Linux。Obscura 可用性受上游平台构建约束，每个平台单独验收；Chrome 连接可用于诊断，不能代替 Obscura 验收。操作和观察有界等待；任务持久化，退出 CLI 不自动销毁浏览器现场；重启后显式观察原任务并先核实状态，不自动重新扣款或提交。详细支持矩阵、默认超时和证据记录在版本文档中。 |
| 登录补充 | 按当前页面提供的方式登录，扫码须交付可查看二维码，并在核实当前身份后通过最终结果、进度事件或调用方显式配置的 HTTP 回调返回成功状态。回调具备事件 ID、HMAC 与独立重试，不包含凭据或个人资料；不绕过人类验证。 |
